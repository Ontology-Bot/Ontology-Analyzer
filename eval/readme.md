# Evaluation 

This folder contains evaluation implementations:
- They ALL use their `.env`, so go and setup them inside individual folders
- To enable/disable service, toggle include in the `docker-compose.yml`
- To run, use `docker-compose up`
- Setup ports in `./.env`