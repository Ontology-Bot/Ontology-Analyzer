PREFIX ex: <http://example.org/plant#>

SELECT ?componentName ?rolePath
WHERE {
  ?st a ex:InternalElement ;
      ex:name "ST101" ;
      ex:hasChild ?c .
  ?c ex:name ?componentName .
  OPTIONAL { ?c ex:rolePath ?rolePath }
}
ORDER BY ?componentName